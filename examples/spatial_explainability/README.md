## spatial_explainability

Run from the repository root after generating the data (`python reproduce.py` or `geoxplain generate-data --out-dir data`):

```bash
python examples/spatial_explainability/run.py
```

Outputs go to `outputs/examples/spatial_explainability/`. The dataset is synthetic; see [data/README.md](../../data/README.md).
