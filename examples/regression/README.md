## regression

Run from the repository root after generating the data (`python reproduce.py` or `geoxplain generate-data --out-dir data`):

```bash
python examples/regression/run.py
```

Outputs go to `outputs/examples/regression/`. The dataset is synthetic; see [data/README.md](../../data/README.md).
