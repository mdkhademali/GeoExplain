## basic_classification

Run from the repository root after generating the data (`python reproduce.py` or `geoxplain generate-data --out-dir data`):

```bash
python examples/basic_classification/run.py
```

Outputs go to `outputs/examples/basic_classification/`. The dataset is synthetic; see [data/README.md](../../data/README.md).
